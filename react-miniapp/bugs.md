# bugs.md — обязательные объяснения

Ответь кратко (2–4 предложения на пункт), своими словами.

1) Почему `push` + `setState(тот_же_массив)` может не вызвать ререндер?

2) Почему `setCount(count + 1)` два раза подряд может дать +1?

3) Как возникает бесконечный цикл в `useEffect` из-за зависимостей?
